# Secrets — never commit
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# OS / editor cruft
.DS_Store
.idea/
.vscode/
*.swp

# Local working files
*.log
*.tmp
pbr_crawler_output.json

# Recruiting xlsx is now auto-synced live from Sheet 2.0 at runtime
# (sheet_sync.py) — no longer needs to be committed, and shouldn't be:
# it's real player PII (contact info, medical/academic data), and every
# commit was a permanent snapshot in git history. Keep data/pbr_rankings.pkl
# tracked (public ranking data, lower sensitivity, no live-sync source yet).
data/recruiting.xlsx
